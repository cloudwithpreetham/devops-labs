apiVersion: v1
kind: Pod
metadata:
  name: app-pod
  labels:
    app: demo-app
    environment: staging
    team: devops
spec:
  containers:
    - name: demo-container
      image: nginx:stable
      ports:
        - containerPort: 80
